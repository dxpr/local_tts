{#
/**
 * @file
 * Template for Local TTS player.
 *
 * Available variables:
 * - button_text: The text for the play button.
 * - show_voice_selector: Whether to show the voice selector.
 * - show_speed_control: Whether to show the speed control.
 * - voices: Array of available voices.
 * - default_voice: The default voice to use.
 * - default_speed: The default speech speed.
 */
#}
<div class="local-tts-player">
  <div class="local-tts-controls">
    <button id="local-tts-play-button" class="local-tts-button" type="button">
      {{ button_text }}
    </button>
    <button id="local-tts-stop-button" class="local-tts-button local-tts-stop" type="button" disabled>
      {{ 'Stop'|t }}
    </button>
  </div>

  {% if show_voice_selector or show_speed_control %}
    <div class="local-tts-settings">
      {% if show_voice_selector %}
        <div class="local-tts-voice-selector">
          <label for="local-tts-voice-select">{{ 'Voice'|t }}:</label>
          <select id="local-tts-voice-select">
            {% for voice in voices %}
              <option value="{{ voice }}" {% if voice == default_voice %}selected{% endif %}>
                {{ voice }}
              </option>
            {% endfor %}
          </select>
        </div>
      {% endif %}

      {% if show_speed_control %}
        <div class="local-tts-speed-control">
          <label for="local-tts-speed-input">{{ 'Speed'|t }}:</label>
          <input
            type="number"
            id="local-tts-speed-input"
            min="0.5"
            max="2.0"
            step="0.1"
            value="{{ default_speed }}"
          >
        </div>
      {% endif %}
    </div>
  {% endif %}

  <div class="local-tts-status" id="local-tts-status"></div>
  <audio id="local-tts-audio" style="display: none;"></audio>
</div>
